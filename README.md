#INFO:

```
--help Show help [boolean]
--version Show version number [boolean]
-m, --multiplier Number by which the multiplicand is to be multiplied.
[number] [required]
-l, --list Shoes the multiplication table up to X10
[boolean] [default: "false"]
-i, --iterations Number of iterations of the multiplication table (10 by
default). [number] [default: 10]

```
